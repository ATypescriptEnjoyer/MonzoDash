import { Entity, Column, EntitySchema, PrimaryGeneratedColumn } from 'typeorm';

@Entity()
export class {{pascalCase name}} {
@PrimaryGeneratedColumn()
id?: string;

@Column()
prop: string;
}

export const {{pascalCase name}}Schema = new EntitySchema<{{pascalCase name}}>({
  name: {{pascalCase name}}.name, target: {{pascalCase name}}, columns: {
  id: {
  type: String,
  primary: true,
  generated: 'uuid'
  },
  prop: {
  type: String,
  }
  }
  });