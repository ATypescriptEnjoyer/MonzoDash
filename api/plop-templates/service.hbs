import { Injectable } from '@nestjs/common';
import { {{pascalCase name}} } from './schemas/{{name}}.schema';
import { StorageService } from '../storageService';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';

@Injectable()
export class {{pascalCase name}}Service extends StorageService<{{pascalCase name}}> {
  constructor(
  @InjectRepository({{pascalCase name}})
  private {{name}}Repository: Repository<{{pascalCase name}}>
    ) {
    super({{name}}Repository);
    }
    }