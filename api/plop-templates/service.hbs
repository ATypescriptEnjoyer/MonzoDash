import { Model } from 'mongoose';
import { Injectable } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { {{pascalCase name}}, {{pascalCase name}}Document } from './schemas/{{name}}.schema';
import { StorageService } from '../storageService';

@Injectable()
export class {{pascalCase name}}Service extends StorageService<{{pascalCase name}}> {
  constructor(@InjectModel({{pascalCase name}}.name) private {{name}}Model: Model<{{pascalCase name}}Document>) {
    super({{name}}Model);
  }
}
