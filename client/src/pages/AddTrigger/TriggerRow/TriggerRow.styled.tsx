import styled from 'styled-components';
import { Select } from '@mui/material';

export const StyledTriggerRow = styled.div`
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
`;

export const StyledSelect = styled(Select)``;
